function decText = huffmanDec(encText, dict)
    % while encoded text still has characters
    while(~isempty(encText))
        for i = 1: length(dict.output)
            if(strcmp(dict.output{i}(position), encText(position)))
    end
end